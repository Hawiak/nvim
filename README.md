# Custom shortcuts

In normal mode:
`cc` = Copy the whole buffer

`ca` Select the whole buffer (Into visual mode)

In visual mode:
`P` = Paste without overwriting the default register (The one that holds the last yanked text)
